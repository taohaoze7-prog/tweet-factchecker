{
  "name": "factchecker-extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc --noEmit && vite build && vite build --config vite.bg.config.ts && vite build --config vite.options.config.ts",
    "typecheck": "tsc --noEmit",
    "package": "npm run build && cd dist && zip -r ../factchecker-$npm_package_version.zip . -x '.*'"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "@types/chrome": "^0.0.270"
  }
}
